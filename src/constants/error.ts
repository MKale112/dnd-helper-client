export enum ValidationErrors {
  'PASSWORD_REQUIRED' = 'Password Required',
  'PASSWORD_TOO_SHORT' = 'Your password needs to be longer than 5 characters.',
  'EMAIL_REQUIRED' = 'Email Required',
  'EMAIL_FORMAT' = 'Your email has an incorrect format.',
}

export enum CharacterCreationErrors {}
